apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: gitlab-registry-db
  namespace: gitlab
spec:
  instances: 1
  storage:
    storageClass: longhorn
    size: 2Gi
  bootstrap:
    initdb:
      database: registry
      owner: registry
