apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: actual-budget
  namespace: actual-budget
  annotations:
    link.argocd.argoproj.io/external-link: https://budget.lab.ciliber.to
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab-gateway
      namespace: envoy-gateway-system
      sectionName: https-lab
  hostnames:
    - budget.lab.ciliber.to
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: '{{ .Release.Name }}-actualbudget'
          port: 5006
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: actual-budget-public
  namespace: actual-budget
  annotations:
    link.argocd.argoproj.io/external-link: https://budget.ciliber.to
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab-gateway
      namespace: envoy-gateway-system
      sectionName: https-public
  hostnames:
    - budget.ciliber.to
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: '{{ .Release.Name }}-actualbudget'
          port: 5006
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /