apiVersion: v2
name: envoy-gateway-deployment
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  # https://hub.docker.com/r/envoyproxy/gateway-helm
  - name: gateway-helm
    version: 1.7.2
    repository: oci://docker.io/envoyproxy